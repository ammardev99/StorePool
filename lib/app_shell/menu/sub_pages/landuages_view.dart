import 'package:flutter/material.dart';

class AppLocalLanguages extends StatelessWidget {
  const AppLocalLanguages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("data"));
  }
}

// define later
